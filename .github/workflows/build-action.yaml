name: build
on: [push]
jobs:
  build-cacom:
    runs-on: debian-latest
    steps:
      - name: Running cmake
        uses: actions/checkout@v3
        run: |
          cd ${{ github.workspace }}
          cd Caby
          mkdir -p build
          cd build
          cmake .. -DCMAKE_BUILD_TYPE=Release
